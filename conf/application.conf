# Copyright 2026 HM Revenue & Customs
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

include "backend.conf"

# This is the main configuration file for the application.
# ~~~~~

appName = disa-returns-submission

# Default http client
play.modules.enabled += "uk.gov.hmrc.play.bootstrap.HttpClientV2Module"

play.modules.enabled += "uk.gov.hmrc.internalauth.client.modules.InternalAuthModule"

# Play Modules
play.modules.enabled += "uk.gov.hmrc.disareturnssubmission.Module"

play.modules.enabled += "uk.gov.hmrc.mongo.play.PlayMongoModule"

play.modules.enabled += "uk.gov.hmrc.objectstore.client.play.modules.ObjectStoreModule"

# The application languages
# ~~~~~
play.i18n.langs = ["en"]

# Router
# ~~~~~
# Define the Router object to use for this application.
# This router will be looked up first when the application is starting up,
# so make sure this is the entry point.
# Furthermore, it's assumed your route file is named properly.
# So for an application router like `my.application.Router`,
# you may need to define a router file `conf/my.application.routes`.
# Default to Routes in the root package (and conf/routes)
# !!!WARNING!!! DO NOT CHANGE THIS ROUTER
play.http.router = prod.Routes

# Controller
# ~~~~~
# By default all controllers will have authorisation, logging and
# auditing (transaction monitoring) enabled.
# The below controllers are the default exceptions to this rule.

controllers {
}

# Microservice specific config

mongodb {
  uri = "mongodb://localhost:27017/disa-returns-submission"
  monthlyReturnTimeToLiveInDays = 20
}

microservice {
  services {
    auth {
      host = localhost
      port = 8500
    }

    internal-auth {
      protocol = http
      host     = localhost
      port     = 8470
    }

    object-store {
      protocol = http
      host     = localhost
      port     = 8464
    }
  }
}

contexts {
  # Runs blocking file-upload work away from the main worker pool.
  file-upload-blocking {
    # Uses a dedicated Pekko dispatcher for blocking file-upload work.
    type = Dispatcher
    # Uses a fixed thread pool rather than the default fork-join executor.
    executor = "thread-pool-executor"
    # Processes one mailbox batch per worker turn to keep blocking tasks fair.
    throughput = 1

    thread-pool-executor {
      # Maximum number of blocking file-upload tasks that can run at once.
      fixed-pool-size = 4
    }
  }
}

internal-auth.token = "valid-internal-auth-token"
object-store.default-retention-period = 1-month
create-internal-auth-token-on-start = true

declarationPeriodStart = 6
declarationPeriodEnd = 19
testOverrideTtlHours = 1

features.strict-z-reference-validation-enabled = true

submission {
  acceptedContentType = "application/x-ndjson"
  maximumContentLength = 1GB
}
