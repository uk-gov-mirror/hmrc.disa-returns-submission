meta {
  name: 07-204-set-clock-outside-declaration-period
  type: http
  seq: 7
}

put {
  url: {{baseUrl}}{{servicePath}}/test-only/overrides
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "zReferences": ["{{clockZReference}}"],
    "clock": {
      "date": "{{outsideDeclarationPeriodDate}}"
    }
  }
}

assert {
  res.status: eq 204
}
